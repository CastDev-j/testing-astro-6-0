import { defineLiveCollection } from "astro:content";
import { z } from "astro/zod";

// const collectionName = defineLiveCollection({
//   loader: CUSTOM_LOADER_FUNCTION (generate this in base of the documentation),
//   schema: Z_TYPE_FOR_THE_LOADED_DATA (similar to content.config.ts schema),
// });

export const collections = {
  // collectionName
};
